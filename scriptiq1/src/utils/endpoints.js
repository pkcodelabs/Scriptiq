const endpoints = {
  postMessageSearch: "/postmessagesearch/",
};
//"http://localhost:3001"
//"https://scriptiqserver.onrender.com"
const baseUrl = "https://scriptiqserver.onrender.com";

const baseUrl1 = "http://localhost:3001";

const CountryUrl = "/core/country/";

const CityUrl = "/core/city/";

export { CityUrl, endpoints, baseUrl, CountryUrl, baseUrl1 };
